# Claude Code — machine-local permissions, not shared
.claude/settings.local.json

# Node — the project has no dependencies, but tools may still create these
node_modules/
npm-debug.log*

# Source archive the site files were delivered in; the files themselves are tracked
files.zip

# Windows / OneDrive noise
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
